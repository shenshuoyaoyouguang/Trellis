# /trellis:hive-activate

> 激活蜂巢并发代理模式，启动蜂群智能协作。

---

## 功能说明

此命令将：

1. **初始化蜂巢状态** - 创建信息素文件，设置初始配置
2. **分析当前任务** - 评估任务复杂度，确定工蜂数量
3. **派遣蜂后 Agent** - 启动任务分解和工蜂调度

---

## 使用方式

```
/trellis:hive-activate [任务描述]
```

**参数**:
- `任务描述` (可选): 要执行的任务描述，如未提供将从当前上下文推断

---

## 执行步骤

### Step 1: 环境检查

```bash
# 检查蜂巢配置
cat .trellis/hive-config.yaml

# 检查开发者身份
python3 .trellis/scripts/get_developer.py
```

### Step 2: 初始化信息素

```python
# 创建信息素文件
python3 .trellis/scripts/hive/pheromone.py init
```

### Step 3: 分析任务

根据任务描述或当前上下文：

1. 识别涉及的文件数量
2. 评估任务复杂度
3. 确定工蜂数量（2-5只）
4. 设置雄蜂比例（默认 40%）

### Step 4: 激活蜂巢

```python
# 激活蜂巢
python3 .trellis/scripts/hive/pheromone.py activate --workers <count>
```

### Step 5: 派遣蜂后

调用蜂后 Agent 进行任务分解：

```markdown
请分析以下任务并分解为独立巢室：

[任务描述]

分解原则：
1. 每个巢室可独立实现和测试
2. 明确的输入/输出边界
3. 依赖关系单向、无循环
```

---

## 示例

### 示例 1: 简单任务

```
/trellis:hive-activate 实现用户登录功能
```

输出：
```markdown
## 🐝 蜂巢激活

**任务**: 实现用户登录功能
**复杂度**: 简单 (2个文件)
**工蜂数量**: 2
**雄蜂比例**: 40%

### 巢室分解

| 巢室 | 描述 | 输入 | 输出 | 依赖 |
|------|------|------|------|------|
| cell-auth | 认证逻辑 | spec/auth.md | src/auth/*.ts | - |
| cell-ui | 登录界面 | spec/ui.md | src/components/Login.tsx | cell-auth |

### 调度计划

1. 立即派遣: worker-001 → cell-auth
2. 等待依赖: cell-ui (等待 cell-auth 完成)

蜂后已启动，正在监控工蜂状态...
```

### 示例 2: 复杂任务

```
/trellis:hive-activate 重构整个用户模块，包括认证、授权、权限管理
```

输出：
```markdown
## 🐝 蜂巢激活

**任务**: 重构整个用户模块
**复杂度**: 复杂 (>5个文件)
**工蜂数量**: 5
**雄蜂比例**: 50%

### 巢室分解

| 巢室 | 描述 | 输入 | 输出 | 依赖 |
|------|------|------|------|------|
| cell-auth-core | 核心认证 | spec/auth.md | src/auth/core/*.ts | - |
| cell-auth-oauth | OAuth集成 | spec/oauth.md | src/auth/oauth/*.ts | cell-auth-core |
| cell-permission | 权限系统 | spec/permission.md | src/permission/*.ts | cell-auth-core |
| cell-api | API路由 | spec/api.md | src/api/*.ts | cell-auth-core, cell-permission |
| cell-ui | 用户界面 | spec/ui.md | src/components/user/*.tsx | cell-api |

### 并行路径

```
cell-auth-core (worker-001)
    ├── cell-auth-oauth (worker-002)
    ├── cell-permission (worker-003)
    └── cell-api (worker-004, 等待依赖)
            └── cell-ui (worker-005, 等待依赖)
```

蜂后已启动，正在监控工蜂状态...
```

---

## 后续操作

激活后，可以使用：

- `/trellis:hive-status` - 查看蜂巢状态
- `/trellis:hive-halt` - 暂停蜂群作业

---

## 注意事项

1. **首次使用**: 确保 `.trellis/hive-config.yaml` 存在
2. **并行限制**: 最大 5 只工蜂同时运行
3. **资源占用**: 每个工蜂会创建独立的 worktree

---

## 错误处理

### 蜂巢已激活

```markdown
⚠️ 蜂巢已处于激活状态

当前状态:
- 活跃工蜂: 3
- 完成巢室: 2
- 进度: 40%

如需重新激活，请先运行 /trellis:hive-halt
```

### 配置缺失

```markdown
❌ 蜂巢配置缺失

请先运行:
trellis init --iflow
```
